services:
  postgres:
    image: postgres:16-alpine
    container_name: crm-hcp-postgres
    environment:
      POSTGRES_DB: crm_hcp
      POSTGRES_USER: crm_user
      POSTGRES_PASSWORD: crm_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U crm_user -d crm_hcp"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:

